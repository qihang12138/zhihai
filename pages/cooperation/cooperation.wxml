<view class="imgs">
    <block wx:for="{{pageData}}" wx:key="index">
        <navigator url="/pages/company/company?id={{item.id}}" class="img-box">
            <image src="{{item.logo}}" class="img" />
        </navigator>
    </block>
</view>